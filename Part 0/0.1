0.1 Summary – HTML Basics
🔹 What is HTML?
HTML (HyperText Markup Language) is the standard language for creating webpages. It provides the structure and content of web documents using a system of elements and tags.

🔹 Creating Your First HTML Document
A basic HTML page includes:

html
Copy
Edit
<!DOCTYPE html>
<html>
  <head>
    <title>My First Page</title>
  </head>
  <body>
    <h1>Hello, world!</h1>
    <p>This is my first HTML page.</p>
  </body>
</html>
<!DOCTYPE html> declares the document type.

<html>, <head>, and <body> define the overall structure.

Tags like <h1> and <p> organize content.

🔹 Embedding Images
Images are inserted using the <img> tag:

html
Copy
Edit
<img src="image.jpg" alt="Description">
src defines the image file path.

alt provides alternative text for accessibility.

🔹 Marking Up Text
Use tags to semantically structure content:

Headings: <h1> to <h6>

Paragraphs: <p>

Emphasis: <strong>, <em>

Lists: <ul>, <ol>, <li>

Example:

html
Copy
Edit
<p>This is a <strong>very important</strong> sentence.</p>
🔹 Creating Links
Use the <a> tag to create hyperlinks:

html
Copy
Edit
<a href="https://example.com">Visit Example</a>
href attribute specifies the URL.

🔹 Conclusion
HTML is the foundation of all web pages. Mastery of basic elements—structure, images, text formatting, and links—is essential for any web developer.

